class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        // run a loop for i = 0 to n-1
        // run a nested loop for j = i+1 to j = n-1
        // this will give all possible combinations of subarrays,
        // from index i to j
        // compare and store max inside the nested loop
        // return the maximum sum
    }
};